cav:
  alpha: 0.01
  beta: 0.05            
  n_targets: 50     
  optimal_init: True

train:
  learning_rate: 0.005
  num_epochs: 500
  batch_size: 32
  train_ratio: 0.9
  random_seed: 42
  num_workers: 0
  device: mps