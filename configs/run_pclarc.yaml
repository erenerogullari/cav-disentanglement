defaults:
  - hardware@experiment: workstation
  - dataset@dataset: celeba_attacked
  - model: vgg16
  - cav_model@cav: pattern_cav
  - _self_

experiment:
  name: pclarc
  random_seed: 42

cav:
  cav_mode: max
  layer: features.28
  alpha: ???
  beta: null
  n_targets: 0
  optimal_init: false
  exit_criterion: none

dataset:
  artifact_type: 
  binary_target:

correction:
  method: PClarc
  layer: ${cav.layer}